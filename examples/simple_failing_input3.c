int writeLine(int a, float c) {
  int var1;
  a = a + 1;
  return a;
}

int mains () {
  set s;
  int pi;
  float pifloat;
  elem el;
  pi = s + 3;

  var1 = 3;
  ~@;

  pi = var1 + var2;
  pi = pi * secretVal;

  writeLine2();

  is_set(s);
  add(pis in s);
  add(2 in s);

  add(1 in s);

  writeLine(pi, s);
  writeLine();
  writeLine(el);
  writeLine(el, s);
}